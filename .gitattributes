# Every text file is LF in the working tree on every platform. The evidence manifest
# hashes committed files and the drift check compares compiled query text byte for byte;
# an autocrlf checkout would break both.
* text=auto eol=lf
*.png binary
*.gif binary
