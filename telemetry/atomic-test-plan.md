# Atomic test plan

Complete this section separately for each detection after checking the current Atomic Red Team catalog.

## Test record template

- Detection ID:
- ATT&CK technique and version/access date:
- Atomic test ID and upstream commit/tag:
- Test description:
- Required privileges:
- Prerequisites/downloads:
- Expected source events:
- Potential network access:
- Cleanup command:
- VM snapshot/rollback point:
- Positive-test time window:
- Negative control:
- Operator review:

## Stop conditions

Do not run a test if its payload, external access, privilege requirement, cleanup, or rollback behavior is unclear. Never substitute the host machine for the disposable test VM.

