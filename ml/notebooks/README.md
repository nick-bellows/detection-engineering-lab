# Notebooks

Add the exploratory anomaly notebook here only after the deterministic detection milestone. Move reusable feature, evaluation, and plotting logic into testable Python modules rather than leaving it solely in a notebook.

